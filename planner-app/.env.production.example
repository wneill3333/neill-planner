# Production Environment Variables Template
# Copy this file to .env.production and fill in your production values

# Firebase Configuration (Production)
# Get these values from your Firebase Console > Project Settings > General
VITE_FIREBASE_API_KEY=your-production-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-production-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=your-production-app-id
VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id

# Google Calendar API (Production)
# Get these from Google Cloud Console > APIs & Services > Credentials
VITE_GOOGLE_CLIENT_ID=your-production-google-client-id.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=your-production-google-api-key

# Application Configuration
VITE_APP_NAME=Neill Planner
VITE_APP_URL=https://your-production-domain.com

# Feature Flags (optional)
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_ERROR_REPORTING=true

# IMPORTANT NOTES:
# 1. Never commit this file with actual values to version control
# 2. Add .env.production to .gitignore
# 3. Set these values in your hosting platform's environment variables
# 4. For Vercel: Add these in Project Settings > Environment Variables
# 5. For Firebase Hosting: Use firebase functions:config:set
# 6. Ensure production Firebase project has proper security rules configured
# 7. Enable Google Calendar API in Google Cloud Console for production
# 8. Add authorized domains in Firebase Console > Authentication > Settings
# 9. Add authorized redirect URIs in Google Cloud Console OAuth settings
